import 'package:flutter/material.dart';

class DashboardAppBarSearchLeading extends StatelessWidget {
  const DashboardAppBarSearchLeading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
    // return Icon(FontAwesomeIcons.search, color: Theme.of(context).accentColor);
  }
}
